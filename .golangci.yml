version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - misspell
    - ineffassign
  exclusions:
    paths:
      - "dist/"

formatters:
  enable:
    - gofmt
