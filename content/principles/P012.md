---
id: P012
number: 12
title: Event-Driven Architecture
category: architecture
severity: critical
weight: 9
applicability:
  project_types: ['api', 'microservices']
  languages: ['all']
---

# P012: Event-Driven Architecture 🔴

**Severity**: Critical

Use async, event-driven patterns for scalability - communicate via events.

**Why**: Prevents resource leaks by ensuring cleanup always happens even when errors occur

**Enforcement**: Skills required - verification_protocol, test_first, root_cause_analysis

**Project Types**: api, microservices
**Languages**: all

**Rules**:
- **No Blocking Calls**: No blocking calls in async functions

**❌ Bad**:
```
@app.post('/api')
def create(data):
    result = blocking_call()  # Blocks!
```

**✅ Good**:
```
@app.post('/api')
async def create(data):
    queue.enqueue(process_job, data)  # Non-blocking
```
