{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cco.dev/schemas/preflight.schema.json",
  "title": "CCO Preflight Command Arguments",
  "description": "JSON Schema for /cco:preflight command argument validation",
  "type": "object",
  "properties": {
    "auto": {
      "type": "boolean",
      "description": "Fully unattended mode - all checks, all scopes, no questions"
    },
    "preview": {
      "type": "boolean",
      "description": "Run all checks, show results, don't release"
    }
  },
  "allOf": [
    {
      "description": "Mutual exclusivity: --preview and --auto are mutually exclusive",
      "not": {
        "required": ["preview", "auto"],
        "properties": {
          "preview": { "const": true },
          "auto": { "const": true }
        }
      }
    }
  ],
  "examples": [
    {
      "description": "Full preflight, unattended",
      "args": { "auto": true }
    },
    {
      "description": "Preview release readiness",
      "args": { "preview": true }
    }
  ]
}
