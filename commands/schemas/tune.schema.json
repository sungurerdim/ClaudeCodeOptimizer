{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cco.dev/schemas/tune.schema.json",
  "title": "CCO Tune Command Arguments",
  "description": "JSON Schema for /cco:tune command argument validation",
  "type": "object",
  "properties": {
    "auto": {
      "type": "boolean",
      "description": "Fully unattended mode - no questions, auto-detect everything"
    },
    "preview": {
      "type": "boolean",
      "description": "Silent validation only, return status (for other commands)"
    },
    "update": {
      "type": "boolean",
      "description": "Skip confirmation, update even if profile exists"
    }
  },
  "allOf": [
    {
      "description": "Mutual exclusivity: --preview and --auto are mutually exclusive",
      "not": {
        "required": ["preview", "auto"],
        "properties": {
          "preview": { "const": true },
          "auto": { "const": true }
        }
      }
    }
  ],
  "examples": [
    {
      "description": "Auto-configure CCO",
      "args": { "auto": true }
    },
    {
      "description": "Validate existing profile",
      "args": { "preview": true }
    },
    {
      "description": "Update existing profile without confirmation",
      "args": { "update": true }
    }
  ]
}
